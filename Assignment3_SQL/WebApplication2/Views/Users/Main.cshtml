@using Entities;
@model List<Folders>
@{
    ViewBag.Title = "Main";
}
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.4.1.min.js"></script>
    <style>


        body {
            font-family: Arial, Helvetica, sans-serif;
        }
        .folders {
             padding: 20px;
             margin:4px 6px 6px 4px;
            background-color:sandybrown;
        }
        /* The Modal (background) */
        .modal {
            display: none; /* Hidden by default */
            position: fixed; /* Stay in place */
            z-index: 1; /* Sit on top */
            padding-top: 100px; /* Location of the box */
            left: 0;
            top: 0;
            width: 100%; /* Full width */
            height: 100%; /* Full height */
            overflow: auto; /* Enable scroll if needed */
            background-color: rgb(0,0,0); /* Fallback color */
            background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
        }

        /* Modal Content */
        .modal-content {
            background-color: #fefefe;
            margin: auto;
            padding: 20px;
            border: 1px solid #888;
            width: 80%;
        }

        /* The Close Button */
        .close {
            color: #aaaaaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }

            .close:hover,
            .close:focus {
                color: #000;
                text-decoration: none;
                cursor: pointer;
            }
    </style>
</head>
<body>

    <div>
        <form action='@Url.Content("~/Users/SignUp")' method="get">
            <button type="submit">Log Out</button>
        </form>
    </div>

    <div>
        <button id="back" onclick="GoBack()">Go Back</button>
        <!-- Trigger/Open The Modal -->
        <button id="myBtn">Create New Folder</button>
    </div>

    <!-- The Modal -->
    <div id="myModal" class="modal">

        <!-- Modal content -->
        <div class="modal-content">
            <span class="close">&times;</span>
                Folder Name:<input type="text" id="folderName" />
                <button type="submit" id="createFolder" onclick="createFolder()">Create</button>
        </div>

    </div>


    <div id="mainDiv">

    </div>


    <script>
        //to go back.............................................................................................
        function GoBack() {
            var d = {};
            var settings = {
                type: "POST",
                dataType: "json",
                url: '@Url.Content("~/Users/OpenParentFolder")',
                data: d,
                success: OpenParent,
                error: OnError
            };
            $.ajax(settings);
            function OpenParent(folders) {
                 $("#mainDiv").empty();
              //  debugger;
                for (var i = 0; i < folders.length; i++) {
                    var btn1 = document.createElement("button");
                    btn1.setAttribute("id", folders[i].ID);
                    btn1.innerText = folders[i].Name;
                    btn1.setAttribute("class", "folders");
                    btn1.style.backgroundColor = "sandybrown";
                    btn1.addEventListener('dblclick', function () { openFolder(this.id) });
                    btn1.addEventListener('click', function () {
                        if (this.style.backgroundColor == "sandybrown")
                            this.style.backgroundColor = "blue";
                        else
                            this.style.backgroundColor = "sandybrown";
                    });
                    $("#mainDiv").append(btn1);
                }
            }
        }
        //....................................................................................................


        //to create new folder...............................................................................
        function createFolder() {
            var folderName = document.getElementById("folderName").value;
            console.log(folderName);
            if (folderName == "") {
                alert("Please enter folder name.");
            }
            else {
                var d = { Name: folderName };
                var settings = {
                    type: "POST",
                    dataType: "json",
                    url: '@Url.Content("~/Users/CreateFolder")',
                    data: d,
                    success: displayFolder,
                    error: OnError
                };
                $.ajax(settings);
                console.log(folderName);
                function displayFolder(id) {
                    var btn1 = document.createElement("button");
                    btn1.setAttribute("id", id);
                    btn1.innerText = folderName;
                    btn1.setAttribute("class", "folders");
                    btn1.style.backgroundColor = "sandybrown";
                    btn1.addEventListener('dblclick', function () { openFolder(this.id) });
                    btn1.addEventListener('click', function () {
                        if (this.style.backgroundColor == "sandybrown")
                            this.style.backgroundColor = "blue";
                        else
                            this.style.backgroundColor = "sandybrown";
                    });
                    $("#mainDiv").append(btn1);
                }
            }
        }
        //........................................................................................................

        //to display folders on first page........................................................................
        $(document).ready(function () {
            //var dataToSend = {"action": "GetFolders","ID":0};
            var Id = { id: 0 };
    var settings= {
				type: "POST",
				dataType: "json",
				url: '@Url.Content("~/Users/GetFolders")',
				data: Id,
				success: mainFolders,
				error: OnError
            };

			$.ajax(settings);
            console.log('request sent');
            function mainFolders(folders) {

                //debugger;
                for (var i = 0; i < folders.length; i++) {
                    var btn1 = document.createElement("button");
                    btn1.setAttribute("id", folders[i].ID);
                    btn1.innerText = folders[i].Name;
                    btn1.setAttribute("class", "folders");
                    btn1.style.backgroundColor = "sandybrown";
                    btn1.addEventListener('dblclick', function () { openFolder(this.id) });
                    btn1.addEventListener('click', function () {
                        if (this.style.backgroundColor == "sandybrown")
                            this.style.backgroundColor = "blue";
                        else
                            this.style.backgroundColor = "sandybrown";
                    });
                    $("#mainDiv").append(btn1);
                }
            }
            console.log("after main");
            function OnError(){
                alert("some error has occured");
            }

        });
        //....................................................................................................


        //function for displaying folders.....................................................................
        function openFolder(id1) {
            var Id = { id: id1 };
    var settings= {
				type: "POST",
				dataType: "json",
				url: '@Url.Content("~/Users/GetFolders")',
				data: Id,
				success: mainFolders,
				error: OnError
            };

			$.ajax(settings);
            //console.log('request sent');
            function mainFolders(folders) {
               // Session["FolderID"]=id1;
                $("#mainDiv").empty();
              //  debugger;
                for (var i = 0; i < folders.length; i++) {
                    var btn1 = document.createElement("button");
                    btn1.setAttribute("id", folders[i].ID);
                    btn1.innerText = folders[i].Name;
                    btn1.setAttribute("class", "folders");
                    btn1.style.backgroundColor = "sandybrown";
                    btn1.addEventListener('dblclick', function () { openFolder(this.id) });
                    btn1.addEventListener('click', function () {
                        if (this.style.backgroundColor == "sandybrown")
                            this.style.backgroundColor = "blue";
                        else
                            this.style.backgroundColor = "sandybrown";
                    });
                    $("#mainDiv").append(btn1);
                }
            }
            //console.log("after main");
           
        };

         function OnError(){
                alert("some error has occured");
        }
        //.............................................................................................
    </script>


    
    <script>
        // Get the modal
        var modal = document.getElementById("myModal");

        // Get the button that opens the modal
        var btn = document.getElementById("myBtn");

        // Get the <span> element that closes the modal
        var span = document.getElementsByClassName("close")[0];

        // When the user clicks the button, open the modal
        btn.onclick = function () {
            modal.style.display = "block";
        }

        // When the user clicks on <span> (x), close the modal
        span.onclick = function () {
            modal.style.display = "none";
        }

        // When the user clicks anywhere outside of the modal, close it
        window.onclick = function (event) {
            if (event.target == modal) {
                modal.style.display = "none";
            }
        }
    </script>

</body>
</html>






